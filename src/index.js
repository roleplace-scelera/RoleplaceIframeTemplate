import React from "react";
import ReactDOM from "react-dom";
import ProfileCodeWrapper from "./ProfileCodeWrapper";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <ProfileCodeWrapper />
  </React.StrictMode>,
  rootElement
);
